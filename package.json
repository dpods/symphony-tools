{
  "name": "composer-quant-tools",
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "download-resources": "node bin/resource-downloader.js",
    "setup": "npm run download-resources",
    "build:major": "npm run setup && node bin/build.js major",
    "build:minor": "npm run setup && node bin/build.js minor",
    "build:patch": "npm run setup && node bin/build.js patch"
  },
  "devDependencies": {
    "adm-zip": "^0.5.12",
    "semver": "^7.6.0"
  }
}
